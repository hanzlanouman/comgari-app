import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Otp = () => {
  return (
    <SafeAreaView>
      <Text>Otp</Text>
    </SafeAreaView>
  );
};

export default Otp;
