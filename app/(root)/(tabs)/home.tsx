import { SafeAreaView, Text } from "react-native";

const Home = () => {
  return (
    <SafeAreaView className="flex-1 bg-white">
      <Text>Home</Text>
    </SafeAreaView>
  );
};

export default Home;
